/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 */

package com.mycompany.aed_aula04_at07;

/**
 *
 * @author devsmarcos
 */
public class AED_AULA04_AT07 {

    public static void main(String[] args) {
        System.out.println("Hello World!");
        double a = Math.sqrt((1/(1*c))-(Math.pow(r, 2))-(4*(Math.pow(c, 2))));
        double b = a/(b-c);
        double c = m*((a*h)+((Math.pow(v, 2))/2));
        double d = (v*t)+ (Math.pow((a*t), 2)/2);
        double e = (p-r)/2;
        double f = (c*(9/5)) + 32;
        double g = (f-32)* (5/9);
    }
}
